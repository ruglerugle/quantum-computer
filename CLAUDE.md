# このリポジトリについて

「量子コンピュータはなぜ速いのか」を、中身の計算の仕組みから理解する学習クエストサイト。
量子ビット（矢印の一覧表）→ 量子ゲート（矢印の回し方・混ぜ方）→ 量子ビットを組み立てる（超伝導実機）→ もつれとCNOT → ドイチュのアルゴリズム → グローバーの探索 → ショアの因数分解 → デコヒーレンスと誤り訂正、の全8ステージ。
公開URL（予定）: https://ruglerugle.github.io/quantum-computer/

**このサイトの主役は「速さの正体＝干渉の設計、を自分の手で確かめること」**。
「量子コンピュータは全部の答えを同時に計算するから速い」という並列計算の神話を天下りに否定するのではなく、
「状態は答え候補ごとの矢印の一覧表。計算は表の書き換え。読み出しは長さの2乗の確率で1つだけ」という道具立てから、
重ね合わせだけでは速くならないこと・干渉でいらない候補を打ち消す設計こそが速さの源であることを、論理の飛躍なく導く。
同時に、量子コンピュータが**何でも速くするわけではない**こと、実用機までの距離（誤り訂正・デコヒーレンス）を誠実に伝える。

# コミュニケーション
- 日本語で応答する（コード・変数名は英語）
- 簡潔に回答し、自明な説明は省略する
- 複雑なタスクでは実装前に計画を提示し、承認後に着手する

# Git規約
- Conventional Commits形式、本文は日本語（例: `feat: ユーザー認証にOAuth2を追加`）
- 確認なしに自動コミット・自動pushしない
- pushするとGitHub Pagesに即反映される。ビルドが走らないことがあるので、
  反映されない場合は `gh api -X POST repos/ruglerugle/quantum-computer/pages/builds` で手動トリガー

# 対象読者（最重要）

**大学レベルの内容を、優秀な中学生にもわかるように解説する**のが目標。説明不足が最大のNG。

- 専門用語（量子ビット・重ね合わせ・振幅・位相・ゲート・ユニタリ・もつれ・オラクル・デコヒーレンス・誤り訂正など）は初出時に必ず中学生向けの言葉で定義する
- 「AだからB」の各ステップを飛ばさない。1つの結論に複数ターンの問答をかけてよい
- 式は導出の途中経過まで書き、記号の読み下しを添える
- 日常のアナロジーを積極的に使う（矢印、一覧表、スイッチ、回転ダイヤルなど）
- 「高校で習った」のような、中学生を除外する前提の言い回しを避ける
- **分量が増えることをためらわない。簡潔さより丁寧さを優先する**

# 内容上の約束（物理）

- 全ステージを貫く道具立ては次の3つだけ。毎ステージ必ずここに帰着させる
  1. **状態＝矢印の一覧表**。答え候補（0/1の並び）1つにつき矢印（振幅）が1本。確率＝矢印の長さの2乗、全候補の合計は1
  2. **計算＝一覧表の書き換え（ゲート）**。長さの合計を保つ回し方・混ぜ方（ユニタリ）だけが許される
  3. **読み出し＝1つだけ**。測定すると候補が1つ選ばれ、手がかりが残った道筋は干渉できない（decoherence-questの足し算ルールと同じ）
- 振幅は「長さと向きを持つ矢印」で通す（complex-quest・decoherence-questと接続）
- **「並列計算だから速い」を肯定的に使わない**。速さの説明は必ず「干渉で、いらない候補の矢印を打ち消し、欲しい候補に強め合わせる設計」に帰着させる
- 誇張しない: グローバーは2次加速のみ、ショアの実用規模には誤り訂正済み論理量子ビットが数千個必要、
  現在の実機は物理量子ビット数百〜千個・誤り率0.1〜1%程度、化学・材料シミュレーションが有力応用、耐量子計算機暗号は標準化済み（2024年NIST）
- ブロッホ球は使わない（1量子ビットは「2本の矢印の表」で通す）

# キャラクターのセリフ

**うさ美 ＝「優秀な中学生」役**
- **知識は中学生レベルまで**。高校以上の事実を「〜でしたね」と既知のように語らせない。それらは必ずねこ博士が教える
- **思考力はずば抜けている**。教わった直後の材料から帰結を導く・矛盾を突く・鋭い質問をする・
  アナロジーを自作する、が彼女の見せ場。「ということは……」で先回りして正しく推論するのは大いに可
- 卑屈・無知アピールはキャラ崩壊。分からないことは堂々と質問する
- 例外: 以下は学習済みという設定。ただし復習として自分で整理し直す形にし、未読の読者にも完結した説明にする
  - quantum-quest（ https://quantum-mechanics.habatakijuku.com/ ）: 二重スリット・光子・測定結果が確率で決まること・波動関数・不確定性原理・シュレーディンガー方程式（井戸型によるエネルギー量子化＝定在波の選別・矢印の時計法則）
  - complex-quest: 複素数（矢印としての掛け算・回転）
  - bell-inequality: もつれ・完全相関・部分系の単独のランダムさ・ベルの不等式と局所実在論の否定
  - decoherence-quest: 足し算ルール（区別がつかない道筋は矢印を足してから2乗）・手がかり・直交・デコヒーレンス
  - cpu_game（論理回路の旅 https://informatics.habatakijuku.com/cpu-logic-game/ ）: 論理回路（AND/OR/NOT）、ゲートを組み合わせて計算を作ること
  - algo-quest（アルゴリズムの旅 https://informatics.habatakijuku.com/algo-quest/ ）: 線形探索・二分探索・バブルソート・クイックソート・ダイクストラ法・動的計画法・計算量

**ねこ博士**
- **oop-game（オブジェクト指向クエスト）のねこ先生と同一人物として口調を合わせる**。落ち着いた短い相づち（「うん」「そう」「そうだね」「その通り」）＋淡々とした説明が基本
- 柔らかい先生口調（「〜ね」「〜なんだ」「〜だよ」「〜かな？」「〜してごらん」「〜してみよう」）
- **マウンティング表現の禁止**：「ヒントをあげよう」「〜は私から言おう」「教えてあげよう」など、教師の優位を誇示する仕切りは使わない。誘導は問いかけそのもので行う
- **称賛は控えめに**：「いい質問だね」「いいところに気づいたね」「その通り」程度まで。「見事」「完璧」「お見事」「大正解」「鋭い」「痛いところを突く」は使わない。相づちとしての「正解。」もうっとうしいので避け、「そう」「その通り」で受ける（クイズ判定UIの「正解！」は対象外）
- 芝居がかった言い回し（「〜なんだ、これが」「では問題――」等）を避ける
- 相手に同意を迫る「〜だろう？」「〜じゃないか？」は使わない（自問の「〜だろう？」は可）
- 用語・事実・歴史はすべて博士が供給する

**共通**
- **図解・アニメーション・シミュレータ（.diagram-box内のSVG等）はあくまで読者向けの補助であり、会話に影響させない**。
  キャラクターに「図を見て」「グラフを見て」「下のシミュレータで」等、ページ上の図を参照するセリフを言わせない。
  直接の参照だけでなく、**図中にしか登場しないラベル・名称・数値をセリフの前提にするのも禁止**。
  会話は図がなくても完結して成立させる（キャラクターが自分で書いた表や計算に言及するのは可）。
  チェック: (1) セリフ内で「図」「グラフ」「年表」「シミュレータ」をgrep、
  (2) 図中の固有ラベルをセリフ内でもgrepし、ヒットしたら会話内で先に定義されているか確認
- 授業型なので「次のステージで」等のメタ言及は許容（物語型ゲームでは禁止）
- セリフを書いたら (1)「だろう？」「じゃないか？」をgrep、
  (2) うさ美の全セリフについて「この知識は中学生が持っているか？ 直前に博士が教えたことからの推論か？」を確認
- **回りくどい前置きを入れない**。疑問は端的にぶつけさせる
- **自明な帰結を長々と論証しない。既出の内容を別の話者が繰り返さない**
- 会話の中で引用（カギ括弧）を使うときは、参照先が直前にあることを確認する

# 表記ルール
- 累乗は `^` ではなく上付き（`2ⁿ`、`10¹⁵`。`長さの2乗`は言葉で）で書く
- 状態は `|0⟩` `|1⟩` のケット表記を使ってよいが、初出時に「『ゼロの状態』と読む記号」と必ず説明する
- 1/√2 は「√2分の1（約0.71）」のように読み下しを添える（初出時）
- 答え候補のビット列は「00」「01」「10」「11」のように等幅で書く
- 検出器・環境に残る記録は decoherence-quest と同じく「手がかり」で統一
- 量子ビットは「量子ビット」（キュービットのルビ・併記は初出時のみ）

# ファイル構成
- `design-system.css` — quest-template由来の共通デザイン。配色は `:root` の変数で管理（回路基板の紺×金）
- `js/progress.js` — 進捗管理・クイズ判定。**クイズの正解は `ANSWERS`**。localStorageキーは `quantumComputerQuestProgress_v4`、グローバルは `QC`
- `stage1.html` 〜 `stage8.html` — 各ステージ。`index.html`（表紙）、`complete.html`（クリア画面）
- `images/` — キャラ画像はWebP（幅300px程度、decoherence-questから流用）。`<img>` には `width`/`height` 属性を必ず付ける。
  favicon.png は流用。ogp.png は未作成
- 図はSVGで手書きし、JSでアニメーションさせる場合は IntersectionObserver でビューポート内のみ再生し、
  `prefers-reduced-motion` で静止画にフォールバックする。アニメは待ち時間を作らず連続的に動かす

# 禁止事項
- README・ドキュメントを勝手に生成・変更しない
- テストコードを確認なしに削除・コメントアウトしない
- 既存の動作するコードを理由なくリファクタリングしない
